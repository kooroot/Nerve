[package]
name = "nerve-core"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
fs2.workspace = true
globset.workspace = true
hmac.workspace = true
libc.workspace = true
nerve-adapter = { path = "../nerve-adapter" }
nerve-config = { path = "../nerve-config" }
nerve-patch = { path = "../nerve-patch" }
nerve-types = { path = "../nerve-types" }
rand.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
subtle.workspace = true
tempfile.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true
ulid.workspace = true
uuid.workspace = true

# H5: Landlock is a Linux-only LSM. The dependency is gated to Linux so non-Linux
# builds neither pull nor compile it; the `nerve_core::sandbox` Landlock code is
# correspondingly `#[cfg(target_os = "linux")]`.
[target.'cfg(target_os = "linux")'.dependencies]
landlock.workspace = true

[dev-dependencies]
async-trait.workspace = true
